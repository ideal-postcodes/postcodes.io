var assert = require("chai").assert,
		path = require("path"),
		helper = require(__dirname + "/helper"),
		Postcode = helper.Postcode;

describe("Postcode Model", function () {
	describe("Seed Postcode Data", function () {

	});
	describe("Load from OSPoint", function () {

	});
	describe("#getPostcode", function () {
		it ("should return postcode with the right attributes");
		it ("should contain Northings and Eastings");
		it ("should contain Longitude and Latitude");
		it ("should contain other Misc information");
	})

});