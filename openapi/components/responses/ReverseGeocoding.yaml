title: Postcode Response
type: object
required:
  - status
  - result
properties:
  status:
    type: integer
    format: int32
    enum:
      - 200
  result:
    type: array
    items:
      oneOf:
        - $ref: '../schemas/ReverseGeocoding.yaml'
    description: 'Data for given geocode'