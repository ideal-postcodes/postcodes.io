title: Postcode Response
type: object
required:
  - message
  - code
  - result
properties:
  result:
    type: array
    items:
      oneOf:
        - $ref: '../schemas/Place.yaml'
    description: 'Data for a given place'
  code:
    type: integer
    format: int32
    enum:
      - 2000
  message:
    type: string
    enum:
      - "Success"
