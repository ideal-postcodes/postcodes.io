title: Geolocated Outcode
description: This Outcode object also contains the distance from the supplied geolocation.
allOf:
  - $ref: "./Outcode.yaml"
  - type: object
    required:
      - distance
    properties:
      distance:
        title: Distance from supplied geolocation
        type: number
        format: double
        description: Distance in metres from specified outcode
        example: 0
